# Common Hooks

This folder contains reusable hooks shared across the project.

Guidelines
- Place new project-level hooks in this folder.
- Feature-specific hooks can live here if reused outside a single component.
- Keep each hook in its own file and name it useXxx.ts.
